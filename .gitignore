# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment & secrets (commit .env.example instead)
.env
.env.local
.env.*.local

# Test coverage & logs
coverage/
*.log

# OS noise
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/settings.local.json

# Local-only: planning docs and the reference build
/reference/
Plan and Architecture to Build StockFlow ERP from Scratch.md
Plan and Architecture to Build StockFlow ERP from Scratch.pdf

# Local safety backups — never publish these (they can contain old history)
.git-backup/

# Skill tooling dropped into the project root — not part of StockFlow
.agents/
resume-builder-skill/
skills-lock.json